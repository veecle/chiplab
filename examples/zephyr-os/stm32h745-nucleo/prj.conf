# The nucleo_h745zi_q/stm32h745xx/m7 board defconfig already enables the UART
# console (CONFIG_SERIAL/CONFIG_CONSOLE/CONFIG_UART_CONSOLE) with
# zephyr,console = &usart3, which Chiplab captures. Nothing to add for a plain
# printf() hello world.
