# The stm32f746g_disco board defconfig already enables the UART console
# (CONFIG_SERIAL/CONFIG_CONSOLE/CONFIG_UART_CONSOLE) with zephyr,console = &usart1,
# which Chiplab captures. Nothing to add for a plain printf() hello world.
