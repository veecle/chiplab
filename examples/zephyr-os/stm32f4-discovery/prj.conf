# The stm32f4_disco board defconfig already enables the UART console
# (CONFIG_SERIAL/CONFIG_CONSOLE/CONFIG_UART_CONSOLE) with zephyr,console = &usart2,
# which Chiplab captures. Nothing to add for a plain printf() hello world.
